{
  "name": "novavei",
  "version": "0.1.1",
  "private": true,
  "description": "NovaVei desktop — Tauri 2 + Luminous Quiet design",
  "type": "module",
  "packageManager": "npm@11.16.0",
  "engines": {
    "node": "24.18.0",
    "npm": "11.16.0"
  },
  "scripts": {
    "dev": "tauri dev",
    "dev:web": "vite --config vite.config.ts",
    "typecheck": "tsc --noEmit",
    "build:web": "vite build --config vite.config.ts",
    "test:frontend": "vitest run --root . src/runtime",
    "test:rust": "cargo test --manifest-path src-tauri/Cargo.toml --lib --no-default-features",
    "lint": "biome lint src scripts vite.config.ts package.json tsconfig.json --error-on-warnings",
    "format:check": "biome check --formatter-enabled=true --linter-enabled=false --assist-enabled=false src scripts vite.config.ts package.json tsconfig.json",
    "format": "biome format --write src scripts vite.config.ts package.json tsconfig.json",
    "verify": "npm run typecheck && npm run lint && npm run test:frontend && npm run test:rust && npm run audit:ipc-capabilities && npm run test:ci-supply-chain && npm run format:check && npm run build",
    "checklist:release-smoke": "node scripts/release-smoke-checklist.mjs",
    "audit:ipc-capabilities": "node scripts/ipc-capability-audit.mjs",
    "test:ci-supply-chain": "node scripts/ci-supply-chain-audit.mjs",
    "audit:portable-packaging": "node scripts/portable-packaging-audit.mjs",
    "build": "tauri build --no-bundle -- --locked",
    "build:installer": "tauri build --bundles nsis -- --locked",
    "pack:portable": "node scripts/portable-packaging-audit.mjs && tauri build --no-bundle -- --locked && node scripts/copy-portable.mjs",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.5",
    "@tauri-apps/cli": "2.11.4",
    "@types/node": "24.13.3",
    "typescript": "7.0.2",
    "vite": "8.1.5",
    "vitest": "4.1.10"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "0.84.1",
    "@earendil-works/pi-ai": "0.84.1",
    "typebox": "1.3.8"
  }
}
